// Can prevent the variable from getting mutated
const someInteger = 10;

// Array
// Note the contents of the array can be mutated despite being a const
const testArray = [2, 1, 31];

// Object
const person = {
    firstName : 'Jamie',
    lastName : 'Jones'
};