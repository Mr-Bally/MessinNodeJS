// Defining an object literal including functions
// A dynamic property setter
// And a simpler assignment for the same named variable

const mystery = "answer"

const PI = Math.PI;

const obj = {
    num1: 120,
    num2: 100,
    f1() {},
    f2: () => {},
    [mystery]: 100,
    PI
}